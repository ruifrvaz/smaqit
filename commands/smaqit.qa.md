---
description: Answer questions about the smaqit framework using its wiki and framework documentation.
---

Invoke the `smaqit-qa` subagent via the Task tool, passing the user's current question as the subagent's input. Relay the subagent's complete answer back to the user, including its source references.
