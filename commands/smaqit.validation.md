---
description: Orchestrate the Validation phase — Coverage specs, then generate and run tests against the deployed system.
---

Invoke the `smaqit-validation` subagent via the Task tool, passing the user's current request and full session context as the subagent's input. Relay the subagent's complete output back to the user, including any clarifying questions it raises.
