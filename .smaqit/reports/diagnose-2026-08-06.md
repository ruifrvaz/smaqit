# Project Diagnose Report — 2026-08-06

## Run Metadata

| Field | Value |
|-------|-------|
| Date | 2026-08-06 |
| Domains Scanned | Testing, Security, Logging, Monitoring, Provisioning, CI/CD; with a focused visual-design toolchain review |
| Total Checks Run | 62 checklist checks, plus 4 focused toolchain checks |
| Total Findings | 5 |
| P1 — Critical | 0 |
| P2 — High | 2 |
| P3 — Medium | 3 |
| P4 — Low | 0 |

> Inventory script: completed successfully. StackProfile: Go installer in `installer/`; no application frontend, container runtime, web server, or deployed-service topology in this repository. Generic application security, logging, monitoring, and backup checks are N/A.

---

## Executive Summary

| Domain | Total | P1 | P2 | P3 | P4 |
|--------|-------|----|----|----|----|
| Testing | 2 | 0 | 0 | 2 | 0 |
| Security | 0 | 0 | 0 | 0 | 0 |
| Logging | 0 | 0 | 0 | 0 | 0 |
| Monitoring | 0 | 0 | 0 | 0 | 0 |
| Provisioning | 3 | 0 | 2 | 1 | 0 |
| CI/CD | 0 | 0 | 0 | 0 | 0 |

The downstream pilot validates the design model's useful granularity: a Functional system-sequence diagram is a distinct customer-to-system black-box view, not a duplicate of a domain model or route-level contract. The failures are toolchain and guidance gaps, not a reason to alter that design profile.

---

## Findings: Testing

| Severity | Check | Affected File | Recommendation | Status |
|----------|-------|---------------|----------------|--------|
| P3 | No automated coverage proves that an installed MCP server is activated by the target agent host and discoverable through ToolSearch. Current smoke coverage verifies generated config and agent metadata only. | `scripts/smoke-test-installer.sh`, `docs/test-cases/visual-design-artifacts.md` | Add a host-level acceptance test where the supported host can be automated; otherwise make an explicit manual release gate that verifies server start, trust, and tool discovery in a newly initialized workspace. | New |
| P3 | `smaqit design validate` and therefore `smaqit validate` return on the first invalid design/specification instead of reporting the complete independent failure set. | `installer/design.go`, `installer/main.go` | Accumulate deterministic per-artifact diagnostics, then return one non-zero result after printing the full list. Preserve fail-fast only for prerequisites that make later checks impossible, such as a missing runtime. | New |

---

## Findings: Security

| Severity | Check | Affected File | Recommendation | Status |
|----------|-------|---------------|----------------|--------|
| N/A | Application boundary checks | N/A | This repository is an installer/framework, not a deployed HTTP application. | N/A |

---

## Findings: Logging

| Severity | Check | Affected File | Recommendation | Status |
|----------|-------|---------------|----------------|--------|
| N/A | Deployed-service log persistence and rotation | N/A | No containerized or deployed application service exists in this repository. | N/A |

---

## Findings: Monitoring

| Severity | Check | Affected File | Recommendation | Status |
|----------|-------|---------------|----------------|--------|
| N/A | Service health, readiness, metrics, and alerting | N/A | No runtime service topology exists in this repository. | N/A |

---

## Findings: Provisioning

| Severity | Check | Affected File | Recommendation | Status |
|----------|-------|---------------|----------------|--------|
| P2 | Managed PlantUML runtime is installed only by `smaqit init`; `smaqit design render`, `validate`, and `mcp plantuml` fail when a valid secondary worktree lacks the ignored `.smaqit/tools/` runtime. | `installer/main.go`, `installer/design.go`, `installer/design_tools.go` | Make the shared runtime-opening path materialize and validate the embedded bundle atomically when absent. Cover a newly created Git worktree without `.smaqit/tools/`; no manual copying. | New |
| P2 | `.vscode/mcp.json` registration is treated as sufficient, but it does not prove the host has loaded, trusted, started, and exposed the server's tools to the current agent session. The downstream ToolSearch failure confirms this gap. | `installer/design_tools.go`, `scripts/smoke-test-installer.sh`, `docs/wiki/workflows/visual-designs.md` | Add a deterministic post-install MCP readiness check and document the supported host activation/trust/restart path. Treat unavailable tools as a clear blocked authoring precondition, not as successful installation. | New |
| P3 | The required lifecycle-rank equality between a design and its least-advanced linked specification is enforced, but the consumer-facing design template and workflow do not state the rule or its `DESIGN-ARTIFACT-STALE` consequence. | `installer/design.go`, `templates/designs/*.template.md`, `docs/wiki/workflows/visual-designs.md` | State the lifecycle coupling next to the template `status` field and in the authoring sequence; route state changes through the existing synchronized specification-status workflow. | New |

---

## Findings: CI/CD

| Severity | Check | Affected File | Recommendation | Status |
|----------|-------|---------------|----------------|--------|
| N/A | Generic application CI/CD gates | `.github/workflows/` | This project has installer smoke and release workflows; deployed-service build, container, and dependency-update checks do not apply to its framework role. | N/A |

---

## Priority Matrix

### P1 — Critical

No P1 findings.

### P2 — High

1. **Bootstrap the managed PlantUML runtime in every worktree on demand** — depends on preserving the existing archive validation, atomic extraction, and narrow Git-ignore ownership contract.
2. **Make MCP activation observable and actionable** — depends on the supported host's configuration, trust, start, and tool-discovery lifecycle; registration alone cannot attest to availability.

### P3 — Medium

1. **Add an MCP host-discovery acceptance gate** — depends on defining the supported host/version and an automatable or explicitly manual release check.
2. **Document lifecycle coupling** — independent guidance improvement after the P2 runtime path is stable.
3. **Aggregate validation diagnostics** — preserve prerequisite fail-fast behavior while reporting every independent artifact failure.

### P4 — Low

No P4 findings.

---

## Already-Tracked Cross-Reference

| Finding | Task ID | Task Title |
|---------|---------|------------|
| None | — | No active task covers these downstream pilot findings. |

---

## Known Findings (Compendium)

| Finding | Compendium Category | Notes |
|---------|---------------------|-------|
| Managed PlantUML dependencies are generated rather than project source | Self-Update | Existing entry covered Git-ignore ownership; it has been updated with the worktree limitation. |
