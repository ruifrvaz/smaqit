---
description: Orchestrate the Development phase — Business/Functional/Stack specs, then a working application.
---

Invoke the `smaqit-development` subagent via the Task tool, passing the user's current request and full session context as the subagent's input. Relay the subagent's complete output back to the user, including any clarifying questions it raises.
